[project]
name = ""
version = "0.0.1"
requires-python = "==3.12"
dependencies = [
    "keras",
    "tqdm",
    "pillow",
    "dlib",
    "opencv-python",
    "jupyter",
    "tensorflow",
    "requests",
    "imutils",
    "matplotlib",
    "scikit-learn>=1.0.2",
]

[dependency-groups]
dev = []

[tool.uv]
package = false

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
